


const initialState = {
  countries: [],
  countriesModifed: [],
  
}

function reducer (state = initialState, { type, payload }){
  switch (type) {
    default:
      return state;
  }
}

export default reducer;
